{
  "name": "captain-liivo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check server.mjs && node --check src/game.js && node --test",
    "start": "node server.mjs",
    "test": "node --test"
  }
}
